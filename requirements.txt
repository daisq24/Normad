# 核心框架
flask[async]==2.2.3
werkzeug==2.2.3  # 固定版本，避免兼容性问题
flask-cors==3.0.10  # 跨域资源共享支持
fastapi==0.95.1
uvicorn==0.22.0
python-dotenv==1.0.0
asgiref>=3.2.0  # Flask async支持

# Azure服务相关
azure-search-documents==11.4.0
azure-cosmos==4.3.1
azure-functions==1.15.0
azure-storage-blob==12.16.0
azure-cognitiveservices-language-textanalytics==0.2.0
openai==0.27.8
pymongo==4.3.3  # MongoDB连接

# 不要指定aiohttp版本，让pip自动解决依赖冲突
aiohttp>=3.6.2

# Bot Framework
botbuilder-core==4.14.3
botbuilder-schema==4.14.3
botbuilder-dialogs==4.14.3
botbuilder-ai==4.14.3
botframework-connector==4.14.3

# 数据处理
pandas==2.0.0
numpy==1.24.3
pydantic==1.10.7

# 工具和实用程序
requests==2.28.2
pyyaml==6.0
python-dateutil==2.8.2
jinja2==3.1.2
gunicorn==20.1.0  # 生产环境WSGI服务器

# 测试工具
colorama==0.4.6  # 终端彩色输出 